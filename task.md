# Задачі до виконання

- [x] 1. **Креслення модулів**:
    - [x] 1.1 Додати `drawing_path` в БД (`ALTER TABLE modules ADD COLUMN drawing_path TEXT`).
    - [x] 1.2 Оновити Pydantic схеми та SQL запити в `main.py` для `drawing_path`.
    - [x] 1.3 Оновити `index.html` (додати `<input type="file" id="mod-file">` у модалки створення та редагування).
    - [x] 1.4 Оновити `app.js` (завантаження файлу при `saveModule` / `submitEditModule` та відображення посилання).
- [x] 2. **Авто-лог зібраних модулів**:
    - [x] 2.1 Оновити UI: при натисканні "Позначити як Зібрано" запитувати кількість годин через `prompt()`.
    - [x] 2.2 Оновити `app.js`: надсилати кількість годин в `completeModule`.
    - [x] 2.3 Оновити `main.py`: якщо статус стає `Зібрано`, створювати/оновлювати запис у `work_logs` (додаючи 1 модуль та передані години).
- [x] 3. **Авто-заповнення рахунку з PDF**:
    - [x] 3.1 Встановити `PyPDF2` (або `pdfplumber`) в бекенді.
    - [x] 3.2 Додати ендпоінт `POST /api/parse-viyar-invoice` в `main.py` (регулярні вирази для суми, номеру, філії).
    - [x] 3.3 Оновити `app.js`: слухати зміну файлу в `#inv-file` та підтягувати дані.
- [x] 4. **Кнопка "Назад"**:
    - [x] 4.1 В `index.html` змінити `onclick` для кнопок "Назад" всередині вкладок об'єкта на `openTab('main')`.
    - [x] 4.2 Верхня кнопка "Назад" має викликати `navigate('projects')`.
- [x] 5. **Опис замовлення**:
    - [x] 5.1 Додано поле `description TEXT` в таблицю `projects` в БД та схеми Pydantic.
    - [x] 5.2 Додано блок опису замовлення в шапку об'єкта (`#pd-description-box`) та поле в модалку редагування клієнта (`#ce-description`).
- [x] 6. **Тип оплати / авансу (% або сума грн)**:
    - [x] 6.1 Додано `payment_type` та `advance_percent` в таблицю `projects` та Pydantic схеми.
    - [x] 6.2 Додано перемикач в модалку редагування клієнта: "Сума (грн)" або "Відсоток (%)" з інтерактивним перерахунком суми авансу та залишку.
    - [x] 6.3 Додано відображення залишку до сплати в картку об'єкта (`#pd-remaining-payment`).
- [x] 7. **Коментування модулів**:
    - [x] 7.1 Створено таблицю `module_comments` (модуль, користувач, текст, час) в SQLite.
    - [x] 7.2 Створено ендпоінти `GET /api/modules/{id}/comments` та `POST /api/modules/{id}/comments`.
    - [x] 7.3 Додано бейдж та кнопку `💬 Коментарі (N)` на картку модуля та модальне вікно перегляду й швидкого додавання коментарів.
- [x] 8. **Графік планування (календар монтажів, зборок і замірів)**:
    - [x] 8.1 Створено таблицю `schedules` в SQLite та REST API ендпоінти для подій календаря.
    - [x] 8.2 Реалізовано сторінку інтерактивного календаря з сіткою днів, перемиканням місяців, фільтрами (`Всі`, `Монтаж`, `Збірка`, `Замір`) та бейджами завдань по днях.
    - [x] 8.3 Реалізовано детальний блок перегляду обраного дня з картками завдань (об'єкт, адреса з посиланням на Google Maps, клієнт, час, виконавець, статус, нотатки) та можливістю зміни статусу й додавання/редагування подій.
